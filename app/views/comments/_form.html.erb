<%= simple_form_for([@article, @comment]) do |f| %>
  <%= f.error_notification id: 'comment_errors_message', class: 'form_errors text-danger' %>
  <div class="d-flex">
      <div class="w-100 my-2 ">

        <%= f.input :content,
         label: "Comments: ", 
         input_html: { rows: 5, class: "form-control "},
         wrapper: false, 
         label_html: {class: "lable "},
          placeholder: "Comment" %>
      </div>

  </div>
  <%= f.button :submit, class: "btn signup-button mb-3 my-1 text-white" %>
  <div>
<% end %>